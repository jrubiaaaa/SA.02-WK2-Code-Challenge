// prompt the user for random numbers to find the prime numbers
var ranNums=prompt("enter the numbers: ")
var arr=input.split(",")
function prime(ranNums){
    var result=[]
    // the basis of the prime numbers
    if(ranNums%ranNums===0 && ranNums/num){
        return result;
    }
    
}
alert(prime(ranNums))